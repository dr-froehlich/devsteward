---
id: REQ-069
title: "Build delivers the validation fixtures it owes — close the REQ-051 pincer so a missing seeded fixture can't deadlock validation"
status: draft
kind: fix
added: 2026-06-29
completed: null
verified_by: null
depends_on: [REQ-051, REQ-030]
concept_refs: []
scenario_refs: []
supersedes: null
tags: [process, validation, fixtures, method, dogfood, postmortem]
---

## Context

REQ-051 made the System Tester's prohibition absolute: a missing lab fixture is a **hard
red**, the Tester **never improvises**, and committing the fixture upstream is *"a human's
call"* — the REQ's own closing line states it *"does not author fixtures for anyone."* That
built one jaw of a pincer. The other jaw was never built: **no rule obliges the builder to
produce the fixture.** The `/advance` Build step (skill §2) says only *"write the code **and**
the acceptance tests named in the REQ's `yaml acceptance` block"* — the `test:` *commands* —
and is silent on the **starting state** those commands run against: the seeded sample project,
the captured fixture, the lab setup that an `artifact`/`manual` acceptance criterion's
validation procedure rests on.

So a `manual`/`artifact` AC that needs a seeded fixture has **no owner under a duty to create
it.** REQ-051 punts it to a human, but no step prompts that human at the one moment it is cheap
(during Build). The gap stays invisible until the System-Test phase, where it detonates: the
only person present (the decoupled Tester) is *forbidden* to fix it, and the validation
deadlocks — a hard red that no later step can clear.

This was surfaced by **dogfooding REQ-068**: its AC6 specified a *seeded multi-lane fixture
project* (one hermetic `regression`, one `live`, one one-time `artifact` test) for a human to
drive a develop checkpoint against. REQ-068's Build never created it; the guided System-Test
session for REQ-068 AC6 came up, found no committed fixture, and correctly stopped — it may not
author one (REQ-051). The deadlock is not REQ-068-specific: it is structural, and it ships to
consumers, because the `/advance` skill is hardlinked into the stamped consumer template.

The fix is the missing production-side duty that mirrors REQ-051's prohibition-side. It is a
**method correction, not new machinery**: the engine already hard-reds a missing fixture at
validate time (REQ-051); we simply make the Build phase deliver it so validation is not
dead-on-arrival.

## Decisions

| # | Decision | Rationale |
|---|----------|-----------|
| 1 | **The Develop (Build) phase owns delivering, committed on `dev`, every validation fixture / seeded sample project / lab starting-state its REQ's `artifact`/`manual` acceptance criteria rest on** — because the decoupled System Tester is forbidden to create them (REQ-051). | Closes the build-side jaw of the pincer. REQ-051 built only the validator-side prohibition; with no matching builder duty, a fixture-needing validation has no owner and deadlocks. The cheap moment to produce a fixture is during Build, while the builder is present and the tree is open. |
| 2 | **Method-only: correct the `/advance` skill §2 (Build) and the handbook workflow** (and, via the hardlink, the stamped consumer template). **No new engine machinery.** | The engine already red-flags a missing fixture at validate time (REQ-051) — the gap is a *duty*, not a *check*. Subtract-don't-patch; the owner's explicit call. The residual that an instruction can be forgotten (the very failure mode here) is acknowledged and accepted as the proportionate cost; a mechanical left-shift is a named follow-on (Notes), not built speculatively. |
| 3 | **Out of scope:** REQ-068's own missing AC6 seeded fixture (it stays pending, handled under REQ-068); and any *left-shift engine tooth* — a develop land-gate that refuses to defer-to-validate while a declared validation fixture is absent (would need a per-REQ fixture/lab declaration surface). | One concern per REQ. REQ-069 fixes the general method; it does not author REQ-068's fixture (that would re-couple the two and grow this past a doc fix), and it does not grow the engine on a need that the method fix may well make moot. |

## Requirement

- The `/advance` skill **Build** step states that when a REQ carries `artifact` or `manual`
  acceptance criteria, the Develop phase must **build and commit on `dev`** every fixture,
  seeded sample project, and lab starting-state those criteria's validation procedures rest
  on — naming explicitly that the decoupled System Tester may **not** create them (REQ-051),
  so an undelivered fixture is a deadlock the Build phase alone can prevent.
- The handbook **workflow** section documents the same build-side duty **next to** the REQ-051
  "fixtures are committed upstream; the System Tester never improvises" paragraph, so the
  prohibition and the production duty are read together as the two jaws of one rule.
- The repo `/advance` skill and the stamped template (`devsteward/templates/.claude/skills/
  advance/SKILL.md`) remain identical (the existing hardlink / sync guarantee), so the duty
  ships to consumers.

```yaml acceptance
- id: AC1
  text: "The stamped `/advance` skill's Build step documents the builder-delivers-validation-fixtures
    duty: when a REQ carries `artifact`/`manual` acceptance criteria, the Develop phase must build
    and commit on `dev` the fixtures / seeded sample project / lab starting-state those validations
    rest on, naming that the System Tester may not create them (REQ-051). Verified by asserting the
    instruction is present in the stamped SKILL.md (hardlinked to the repo skill)."
  test: "python -m pytest tests/test_advance_skill_contract.py::test_build_delivers_validation_fixtures"
  check: regression
  status: pending
- id: AC2
  text: "The handbook workflow section documents the build-side fixture-delivery duty adjacent to
    the REQ-051 fixtures-committed-upstream / never-improvise paragraph, so the validator prohibition
    and the builder production duty are presented as one rule. Verified by asserting both the REQ-051
    paragraph and the new build-side duty are present in `_03-workflow.qmd`."
  test: "python -m pytest tests/test_handbook_taxonomy.py::test_workflow_documents_builder_owns_validation_fixtures"
  check: regression
  status: pending
```

## Notes

- **Why method-only is honest here.** The oracle for both criteria is *"does the doc state the
  duty"* — coupled and hermetic, so `regression` is the truthful lane (no fixture of its own
  required, which is fitting for the REQ that exists to stop fixtures from being forgotten).
- **Residual + the named follow-on.** An instruction can be ignored, which is exactly how this
  gap arose. If forgetting recurs after this lands, the next step is a *left-shift engine tooth*
  — the develop land-gate refuses to defer a REQ to its System-Test phase while a declared
  validation fixture/lab asset is absent (firing REQ-051's check at develop-land instead of
  validate-time). That needs a per-REQ fixture declaration surface (extend `process.lab`) and is
  a follow-on REQ, **not** built speculatively now.
- **REQ-068 AC6** stays pending; its seeded multi-lane fixture is REQ-068's to deliver (or to
  resume once produced under this corrected method). REQ-069 does not author it.
- **Localization:** all skill/handbook/template text is technical English; no user-facing
  localized strings introduced.
