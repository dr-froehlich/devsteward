---
id: REQ-052
title: Review after the REQ-047 migration — code review + skill review for stale/bent legacy
status: draft            # draft | open | in-progress | blocked | done | dropped | superseded
kind: chore             # feature | fix | chore | refactor | spec | design | docs
added: 2026-06-19
completed: null
verified_by: null
depends_on: [REQ-047]
concept_refs: []
scenario_refs: []
supersedes: null
tags: [review, cleanup, migration]
---

## Context

This requirement describes a review activity *after* the major overhaul of DevSteward
introduced by REQ-047 (the trunk-based, single-source-of-truth pivot landed across
REQ-048…051). It captures Peter's desire to make this tool productive, stable, and
efficient on the first attempt without major reworks.

> **Stub — needs intake.** This was a raw idea stub; it was given valid draft frontmatter
> and renumbered from REQ-051 to REQ-052 when the lab-fixtures Stage D of REQ-047 took the
> REQ-051 id the plan had reserved for it (the plan + the landed REQ-049/050 cross-refs
> already named REQ-051 for lab fixtures). Run `/intake REQ-052` to harden it into a real
> requirement with acceptance criteria before it becomes active.

## Requirement

### Code review

After a major overhaul, stale code segments sometimes remain — inconsistencies, an
architecture bent by the target change, or legacy elements and structure that still
determine (parts of) the code.

Look for those elements and fix them if feasible and low risk; otherwise raise them in the
review report with recommendations on how to address them.

### Skill review

(To be detailed at intake — review the skills against the trunk-based, single-source-of-truth
model for stale assumptions left over from the feature-branch/worktree era.)

## Notes

None yet — to be filled at intake.
