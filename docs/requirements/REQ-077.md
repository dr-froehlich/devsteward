---
id: REQ-077
title: The checkpoint commit is atomic — the status flip rides the code commit, a leftover is caught, and the ledger↔marker guard is symmetric
status: draft
kind: fix
added: 2026-07-09
completed: null
verified_by: null
depends_on: [REQ-018, REQ-028, REQ-032, REQ-063, REQ-076]
concept_refs: []
scenario_refs: []
supersedes: null
tags: []
---

## Context

`STEWARD.md` (the shipped manual, line 214) promises that a REQ's frontmatter `status:`
flip, its `REQUIREMENTS_INDEX.md` row edit, and the code that satisfies it move in the
**same commit** — "the engine does this for you on a checkpoint." FlowSteward observed the
engine break that promise twice back-to-back on 2026-07-09: `steward checkpoint REQ-098`
(commit `3a71b62`, hand-fixed in `ecb49d3`) and `steward checkpoint REQ-099` (commit
`070d8c3`) each landed the code + tests + ledger while leaving the `status: draft → done`
frontmatter flip and the index-row `DRAFT → DONE` edit **written to the worktree but
uncommitted** — silently, with no error. Both were caught only because a human ran `git
status` out of habit. Full writeup: `flowsteward/docs/postmortems/2026-07-09-checkpoint-omits-status-flip.md`.

The flip *is* produced — `ReqDoneFlipper` (`profiles/req/checkpoint.py`) runs as the
`on_verified` seam *before* the commit and writes `status: done` to the REQ file and `DONE`
to the index (the worktree diff in the postmortem confirms the write happened). What fails
is **staging**: the flip is written but never lands in the commit. The prime suspect is
REQ-076's boundary-delta staging: `GitCli._stage_code(baseline)` rebuilds the index from
`HEAD` and stages only `dirty_paths() - baseline` — so when the REQ file or index path is
in the boundary set (a headless authoring session that touched it, or any path dirty before
the transaction boundary), `on_verified`'s subsequent flip to those same paths is subtracted
back out and left unstaged. The `checkpoint` path (`baseline is None`, whole-tree
`git add -A`) should be immune by that reading, yet the field reproduced it there too — so
develop must confirm the exact mechanism, not assume it. The fix is scoped to the observable
contract, not to one theory of the cause: **after a green checkpoint/land, the flip is
committed and the tree is clean.**

Two guards failed to catch it. First, nothing asserts a clean tree after the land returns
success (REQ-032 promises "every terminal step outcome leaves a clean tree" but does not
verify it at the checkpoint tail). Second, the marker↔ledger reconciliation lint (REQ-028
AC5, `lint.py` rule 7) is **one-directional**: it fires only when frontmatter is `done` but
the ledger is not (marker-ahead) — line 177 `continue`s past every REQ whose frontmatter
is not `done`, so the *opposite* drift (ledger `done`, committed marker still `draft`) is
structurally invisible. And it reads the *working tree*, where the uncommitted flip makes
everything look consistent, so it cannot see a committed-HEAD-vs-worktree gap at all. This
same reconciliation was the top corrective action of the 2026-06-09 REQ-003a postmortem;
it landed (REQ-028) but only in the direction that postmortem's failure took.

If a checkpoint's dirty leftover is ever discarded (a `git clean`, a stash drop, a branch
tidy) before the human notices, the REQ silently reverts to reading `draft`/`DRAFT` in the
committed docs while the ledger and the shipped code say `done` — an unreconciled false
`draft` with no writer to correct it.

## Decisions

| # | Decision | Rationale |
|---|----------|-----------|
| 1 | Fix to the **contract**, not one theory of cause: a green checkpoint/land commits the flip (frontmatter + index) in the code commit and leaves a clean tree. | The static read of the `checkpoint` path says it should already work, yet the field reproduced the drift there. Pin the observable, let develop root-cause the staging. |
| 2 | Cover **both** land tails — interactive `steward checkpoint` (`baseline is None`) and the headless `mechanical_land` scoped-staging path (`baseline` set). | They share `_land_checked`; the scoped path is the prime suspect and the interactive path is where the field caught it — a real fix survives both. |
| 3 | Add a **post-land clean-tree assertion** at the checkpoint/land tail: a green land that leaves anything unstaged (modulo `.devsteward/`) fails loudly, it does not return DONE. | Strengthens REQ-032 from a promise into a checked invariant at the one tail that broke it — the immediate detector a human's `git status` habit was standing in for. |
| 4 | Make the marker↔ledger lint guard **symmetric** and read **committed HEAD**: hard-error when the ledger records a REQ's delivering step DONE but HEAD's frontmatter/index still read not-done, in addition to the existing marker-ahead case. | The durable backstop the 2026-06-09 postmortem asked for, in the direction this drift takes. Reading HEAD (not the worktree) is the only source that catches a flip that was written-but-not-committed. |
| 5 | Do **not** add new git topology or a second commit — the flip already belongs in the code commit; this is a staging/assertion fix, not new machinery. | House rule: prefer subtraction; a string of fixes that each add structure is a smell. The one commit is right; make it complete. |

## Requirement

After a green `steward checkpoint REQ-NNN develop` (and the headless `mechanical_land`
equivalent) on a REQ whose develop step lands:

- The **one authoritative code commit** at `HEAD` contains the REQ frontmatter `status: done`
  flip **and** the `REQUIREMENTS_INDEX.md` row `DONE` edit, together with the code/tests —
  never written to the worktree as an after-the-fact, uncommitted side effect.
- The working tree is **clean** (`git status --porcelain` empty apart from the trailing
  `.devsteward/` ledger commit the engine makes itself). A land that would leave the flip (or
  any tracked edit) unstaged **fails loudly** — it does not report success with a dirty tree.
- `steward lint` **hard-errors** when the ledger records a REQ's delivering step (`develop`,
  or legacy `land`) as DONE but the REQ's **committed** frontmatter status / index row still
  read not-`done` — the symmetric complement of the existing marker-ahead rule — reading
  committed HEAD so a written-but-uncommitted flip in the worktree cannot mask the drift.

```yaml acceptance
- id: AC1
  text: >-
    Driving a real `steward checkpoint` (baseline is None) on a fresh landing REQ through the
    executor against a real git repo produces a HEAD commit whose committed REQ frontmatter
    reads `status: done` and whose committed index row reads `DONE`, and leaves
    `git status --porcelain` (excluding `.devsteward/`) empty. The flip is IN the commit, not
    the worktree.
  test: "python -m pytest tests/test_req077_atomic_flip.py::test_checkpoint_commits_flip_clean_tree"
  check: regression
  status: pending
- id: AC2
  text: >-
    The headless land path (`mechanical_land` with a session boundary `baseline` set, the
    scoped-staging suspect) likewise commits the frontmatter + index flip into the code commit
    and leaves a clean tree — even when the REQ file / index path is inside the boundary delta
    (the exact condition REQ-076's `dirty_paths() - baseline` subtraction would otherwise drop).
  test: "python -m pytest tests/test_req077_atomic_flip.py::test_headless_land_scoped_staging_includes_flip"
  check: regression
  status: pending
- id: AC3
  text: >-
    A green land that would leave a tracked edit unstaged after the code commit (the observed
    drift, injected) fails loudly — the engine reports the dirty tree / raises rather than
    returning DONE with an uncommitted flip. A clean land returns DONE; a leftover does not
    pass silently.
  test: "python -m pytest tests/test_req077_atomic_flip.py::test_land_asserts_clean_tree_or_fails_loudly"
  check: regression
  status: pending
- id: AC4
  text: >-
    `steward lint` hard-errors (non-zero, naming the REQ) when the ledger records a REQ's
    develop (or legacy land) step DONE but the committed frontmatter/index still read
    draft/DRAFT — the new ledger-ahead direction — while the pre-existing marker-ahead case
    (frontmatter done, ledger not) continues to fire. Both directions are covered.
  test: "python -m pytest tests/test_lint_marker_ledger.py::test_ledger_done_but_marker_draft_hard_errors"
  check: regression
  status: pending
- id: AC5
  text: >-
    The reconciliation guard reads committed HEAD, not the working tree: with the flip written
    to the worktree (so on-disk frontmatter looks `done`) but HEAD still at `draft` and the
    ledger DONE, lint STILL flags the drift — proving a written-but-uncommitted flip cannot
    mask it. A genuinely committed-and-consistent REQ lints clean.
  test: "python -m pytest tests/test_lint_marker_ledger.py::test_reconcile_reads_committed_head_not_worktree"
  check: regression
  status: pending
```

## Notes

- **Every AC is `regression`:** the oracle is `git` and the engine's own recorded state —
  decoupled from any mock, runs headless in every develop gate, no lab and no live service.
  There is no `artifact`/`manual` check, so this REQ has no System-Test phase.
- **Prime suspect, not a mandate:** Decision 1 pins the contract; develop confirms whether the
  cause is REQ-076's `_stage_code` boundary subtraction, an `on_verified`/`_commit` ordering
  issue, or both. The fix must make AC1 **and** AC2 pass — if only the scoped path was broken,
  the interactive-path test still guards against a regression.
- **Interaction with REQ-063's pre-commit capture gate:** `on_verified` runs after
  `_capture_gap` and before `_commit`; the fix must not perturb the capture-gate tree (the gate
  checks pure code, and the flip is not a source/test file the named tests read) — the clean-tree
  assertion runs on the *certified* land tail, not the gap path (a gap already sets FAILED and
  leaves the ledger-close commit as the only write).
- **Out of scope:** any new commit, branch, or worktree; the `.devsteward/` ledger's own
  trailing commit (unchanged, REQ-032/048); the `commit_deferred` (validate-pending) path, which
  intentionally does not flip — only the *landing* tail is in scope.
