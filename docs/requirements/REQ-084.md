---
id: REQ-084
title: "The doc layout is the consumer's, not the engine's — config seams for plans_dir/concepts_dir honored by the gates, the stamp and the stamped skills; and the operator-only onboard skill leaves the stamped set"
status: done
kind: fix
added: 2026-07-30
completed: null
verified_by: null
depends_on: [REQ-024, REQ-029, REQ-036, REQ-039, REQ-066]
concept_refs: []
scenario_refs: []
supersedes: null
tags: [config, scaffolding, templates, onboarding]
---

## Context

Onboarding **recipes** (a fork of the upstream Tandoor recipe manager) exposed two gaps in
the same surface: what the engine forces into a consumer's tree.

**1. The doc-layout seam is half-built.** recipes publishes `docs/` with mkdocs — everything
under it is built and deployed by a docs workflow — so the DevSteward corpus must live
*outside* `docs/`. `requirements_dir`/`index_file` are configurable, so recipes set
`requirements_dir: requirements` and its REQ corpus landed correctly at the repo top level.
`plans_dir` and `concepts_dir` are **not**: they are hardcoded properties
(`config.py:136-146`, `root/"docs"/"plans"` and `root/"docs"/"concepts"`) with no config
key. The consequences are concrete and already visible in recipes: `/onboard` step 3 (which
hand-merges the scaffolding — it does *not* run `steward new`) placed the corpus correctly at
`requirements/` because a key existed to honor, and dropped `docs/plans/README.md` straight
into the mkdocs tree because none did. The two
land gates — `PlanArtifactGate` and `ConceptArtifactGate` — can only ever look at
`docs/plans`/`docs/concepts`, so a plan written where this project keeps its plans would be
refused. The stamped skills make it worse: `/intake`, `/advance`, `/bootstrap` and
`STEWARD.md` instruct sessions to write literal `docs/plans/NNNN-*.md` and
`docs/concepts/REQ-NNN.md`, so even after the config key existed, a consumer session would
keep writing into the wrong tree and the gate would keep refusing. Half a seam is worse than
none: it looks configurable, and silently isn't.

**2. `steward sync` ships an operator tool to consumers.** `skillsync`'s tracked set is
engine-derived — whatever skill directories `templates/.claude/skills/` ships (REQ-036/066,
deliberately, so no hand-maintained list can fall out of step). `onboard` sits in that
directory, so every stamped project receives `/onboard` and every `steward sync` keeps it
current — while the skill's own first paragraph says it "is an **operator tool**: run by the
person performing the migration … *not* shipped to consumers (a stamped project is already
onboarded and never runs this)." Doctrine and machinery contradict each other; the machinery
wins, and consumers carry a skill that can only mislead a session (it orchestrates migrating
a project that is, by construction, already migrated).

This is a **regression against a standing decision**, not a new call: REQ-024 Decision 2 ruled
that onboard "Lives in DevSteward's **own** `.claude/skills/`, not in `templates/` … Bundling
it would ship dead scaffolding" — and the same develop commit that recorded it (`c7ce317`)
added the template copy anyway. `tests/test_onboard_skill.py`'s docstring has described the
correct state in prose ever since, with nothing asserting it.

Both are the same class of defect and are fixed together in one REQ: the engine deciding a
consumer's tree from the engine's own conventions rather than from the consumer's config.

## Decisions

| # | Decision | Rationale |
|---|----------|-----------|
| 1 | **Add the two seams (symmetry), don't subtract the existing ones.** `plans_dir` and `concepts_dir` become `config.yaml` keys defaulting to `docs/plans`/`docs/concepts`, loaded exactly like `requirements_dir`. | REQ-083 retired `roadmap_file` because nothing read it; these two are read by live land gates and a real consumer (recipes) needs to relocate them. The asymmetry — three of five doc paths configurable — is the defect, and symmetry is the smaller change than de-configuring `requirements_dir` and breaking recipes. |
| 2 | **The `/onboard` skill owns doc placement; `steward new` is not touched** (corrected at develop, 2026-07-30 — the intake version of this decision rested on a false premise, that `/onboard` step 3 runs `steward new --force`; the skill states the opposite and hand-merges instead). Step 3 writes the doc-path keys into `.devsteward/config.yaml` **before** copying scaffolding, then places every artifact at the configured paths. | The recipes evidence fits exactly: the corpus honored `requirements_dir`, only plans (the keyless path) went astray — the session follows the config where one exists. And a greenfield `steward new` target *cannot* carry a config before the stamp writes one, so teaching the stamp to honor a pre-existing config buys machinery with no present consumer (owner's ruling at the fork). |
| 3 | **The stamped prose reads the config, never a literal path.** `/intake`, `/advance`, `/bootstrap` and `STEWARD.md` name "the configured plans/concepts dir" and how to find it, instead of `docs/plans/`. The handbook chapters follow. | Without this the seam is half-wired in a new way: the engine would look in the right place while every session writes to the wrong one. The gate's refusal is the only thing that would catch it — a paid session lost to a documented-wrong path. |
| 4 | **The gates' refusal names the configured relative path**, not `Path.name`. | With `plans_dir: requirements/plans`, today's message would say "no file in `plans/`" — ambiguous exactly when the layout is unconventional, i.e. exactly when the operator needs to be told where the engine actually looked. |
| 5 | **`onboard` stays in `templates/` and is filtered by name** — a one-entry `OPERATOR_ONLY = {"onboard"}` constant in `skillsync`, honored by `steward new`'s stamp and by the tracked set (sync/status/lock). Corrected at develop (2026-07-30): intake said "move it out of `templates/`", which is unbuildable as stated. | The repo's `.claude/skills` is a **single symlink** to `devsteward/templates/.claude/skills` — the operator's skill path and the shipped template tree are one directory, not a hardlinked pair, so there is no second copy to delete: removing it from `templates/` removes `/onboard` from DevSteward itself (observed). The alternatives were a per-skill-symlink restructure (owner: no) or a frontmatter marker (a non-standard key in Claude Code's schema for one skill). A named exclusion pinned in both directions by a meta-test is the simplest thing that works — owner's ruling, "keep it simple". |
| 6 | **Already-stamped consumers keep their stray copy; sync just stops tracking it.** The engine never deletes a consumer file. `drift`/`steward status` stay silent about it (it is no longer engine-owned), and the next `sync` writes a lock containing only tracked artifacts, so the dead `onboard` key is pruned. | Owner's call. Giving `sync` delete-power over consumer files to retire one skill is machinery bought for a one-off; the three known consumers (FlowSteward, memzy, recipes) delete the directory by hand whenever they next touch `.claude/`. |
| 7 | **Legacy configs stay loadable by construction** — the new keys are read with `data.get(...)` defaults, so every existing consumer config keeps working with no migration verb. | Same lenient-parsing contract as REQ-083 decision 2. |

## Requirement

1. **Config seam.** `Config` carries `plans_dir: str = "docs/plans"` and
   `concepts_dir: str = "docs/concepts"` as loaded fields (both read from `config.yaml`
   via `load_config`), each with a root-resolved `Path` property — `plans_path` /
   `concepts_path`, following the existing `requirements_dir → req_dir`, `index_file →
   index_path` split (a dataclass cannot carry a field and a property of one name).
   Absent keys resolve to the current defaults. The stamped `config.yaml.tmpl` documents
   both keys alongside `requirements_dir`, naming the mkdocs-style case they exist for.

2. **Gates.** `PlanArtifactGate` and `ConceptArtifactGate` operate over the configured
   directories, and every refusal message quotes the configured path **relative to the
   repo root** (e.g. `requirements/plans/`), not just the final path segment.

3. **Placement (the `/onboard` skill).** Step 3 of the operator skill writes the target's
   doc-path keys — `requirements_dir`, `index_file`, `plans_dir`, `concepts_dir` — into
   `.devsteward/config.yaml` **before** any scaffolding is copied, and then places every
   doc artifact at those configured paths, never under `docs/` unless the config says so.
   `steward new` is unchanged (it stamps a greenfield tree, which by construction has no
   config to honor); engine-owned artifacts (`.claude/skills/**`, `STEWARD.md`) keep their
   fixed relative paths in both paths — they are engine behaviour, not project docs.

4. **Stamped prose.** No stamped surface hardcodes a doc-tree path: the shipped
   `/intake`, `/advance`, `/bootstrap` skills and `STEWARD.md` refer to the configured
   requirements/plans/concepts directories and tell the reader where to find them
   (`.devsteward/config.yaml`). DevSteward's own handbook chapters (`_01`, `_02`, `_03`)
   are updated the same way; the conventional `docs/…` layout may be named as the
   *default*, never as the location.

5. **Onboard leaves the stamped set.** `onboard` is named operator-only in one place
   (`skillsync.OPERATOR_ONLY`): `steward new` does not stamp it, `bundled_skill_names` /
   `tracked_artifacts` exclude it, so `sync`/`status` never track or refresh it. The file
   stays where it is — that directory is also DevSteward's own `.claude/skills` — and the
   other four skills ship exactly as before. Its prose is updated per requirement 4,
   including the doc-path keys it must write into the target's config *before* placing any
   scaffolding.

6. **Stray copies are inert.** For a consumer that already holds `.claude/skills/onboard/`
   and an `onboard` entry in `.devsteward/stamped.lock`: the file is left untouched,
   `drift()`/`steward status` report nothing about it, and the next `sync` writes a lock
   whose keys are exactly the tracked artifacts (the dead key pruned).

```yaml acceptance
- id: AC1
  text: >
    Config regression — load_config over a config.yaml carrying plans_dir and
    concepts_dir (both non-default, e.g. requirements/plans and requirements/concepts)
    yields root-resolved paths at those locations; a config omitting both keys yields
    docs/plans and docs/concepts; a legacy config carrying neither key still loads.
  test: "python -m pytest tests/test_req084_doc_layout_seam.py::test_config_keys"
  check: regression
  status: pending
- id: AC2
  text: >
    Gate regression — over a tmp repo whose config names a non-default plans_dir and
    concepts_dir, PlanArtifactGate and ConceptArtifactGate accept a plan/concept
    deliverable at the configured location and refuse an otherwise-identical one sitting
    at docs/plans / docs/concepts; each refusal message contains the configured path
    relative to the repo root, not merely its last segment.
  test: "python -m pytest tests/test_req084_doc_layout_seam.py::test_gates_follow_config"
  check: regression
  status: pending
- id: AC3
  text: >
    Placement regression over the operator onboard skill's documented procedure — its
    stamp step tells the session to write the four doc-path keys (requirements_dir,
    index_file, plans_dir, concepts_dir) into .devsteward/config.yaml before copying any
    scaffolding, and to place doc artifacts at the configured paths rather than under a
    literal docs/ tree.
  test: "python -m pytest tests/test_req084_doc_layout_seam.py::test_onboard_skill_places_docs_by_config"
  check: regression
  status: pending
- id: AC4
  text: >
    Meta-test over the installed package — no stamped surface hardcodes a doc-tree
    path: every templates/.claude/skills/*/SKILL.md and templates/STEWARD.md is free of
    the literals `docs/plans` and `docs/concepts` (and of `docs/requirements` outside an
    explicitly-labelled default), so the pin cannot be lost to a later template edit.
  test: "python -m pytest tests/test_req084_doc_layout_seam.py::test_no_hardcoded_doc_paths_in_stamped_prose"
  check: regression
  status: pending
- id: AC5
  text: >
    Onboard does not ship — bundled_skill_names()/tracked_artifacts() over the installed
    templates root exclude onboard while still naming the four consumer skills, and a
    `steward new` stamp produces no .claude/skills/onboard directory or file while
    producing the other four; the operator's copy is still readable through DevSteward's
    own .claude/skills/onboard/SKILL.md and still declares itself an operator tool
    (REQ-024 Decision 2, restored).
  test: "python -m pytest tests/test_req084_onboard_untracked.py::test_onboard_left_the_stamped_set"
  check: regression
  status: pending
- id: AC6
  text: >
    Stray-copy regression — for a fixture consumer holding .claude/skills/onboard/SKILL.md
    and an `onboard` key in .devsteward/stamped.lock, drift() returns no record naming
    onboard, sync() neither refuses nor refreshes it and leaves the file's bytes
    untouched, and the lock sync writes afterwards has exactly the tracked-artifact keys
    (no `onboard`).
  test: "python -m pytest tests/test_req084_onboard_untracked.py::test_stray_onboard_is_inert"
  check: regression
  status: pending
```

## Notes

- **Why no artifact/manual check.** Both halves are pure-Python and fully observable
  headless — config loading, gate refusal text, the stamped tree, the tracked set. The
  recipes repo is the motivating case, not the oracle; re-stamping it is an operator chore
  once this lands, not this REQ's proof. (Owner's call at intake: regression only.)
- **recipes' own cleanup is consumer-side and out of scope:** setting `plans_dir` in its
  config, deleting the stray `docs/plans/README.md` from the mkdocs tree, and removing its
  stamped `/onboard` copy are chores in that repo. Same for FlowSteward and memzy's stray
  `/onboard` (decision 6).
- **The repo's skill path is a symlink, not a hardlink pair** (established while building):
  `.claude/skills` → `devsteward/templates/.claude/skills`, one git blob of mode `120000`.
  Every "repo skill" *is* the template file; there is no second copy anywhere. Anything that
  wants a skill present for the operator but absent from consumers must therefore filter at
  stamp/track time (decision 5) — deleting from `templates/` deletes it from DevSteward too.
- **Out of scope (genuine non-goals):** a `docs_dir`-style single base key that
  requirements/plans/concepts hang off (three explicit keys keep each path independently
  placeable, which is what a fork with a pre-existing tree needs); any `audience:`/skill
  visibility classifier (decision 5); any sync power to delete consumer files (decision 6);
  and any migration verb for legacy configs (decision 7).
- **Doctrine touched:** this is the "minimal topology / prefer subtraction" principle cutting
  both ways in one REQ — the seam is *added* because a live gate reads it and a real consumer
  needs it, while the operator skill is *subtracted* from the stamped set rather than gated by
  a new classifier.
