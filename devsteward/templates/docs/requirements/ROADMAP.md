# Roadmap

The dependency DAG over the requirements — the order work becomes eligible. The engine
derives step eligibility from `depends_on`; this file is the human-readable view of the
same graph, plus near-term intent.

## Now

- REQ-001 — north star (the compass; never "done" in the usual sense)

## Next

_(Add REQs here as `/intake` produces them. A REQ becomes eligible once every REQ in its
`depends_on` is done.)_

## Dependency graph

```
REQ-001
```
